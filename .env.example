# Local dev defaults. Override per-env with real values in .env.local (gitignored).
POSTGRES_USER=priorart
POSTGRES_PASSWORD=local-dev-password
POSTGRES_DB=priorart
POSTGRES_PORT=15433
API_PORT=18000
