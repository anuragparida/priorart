# PriorArt

> A self-hosted startup-idea deduplication & competitor-research service. Paste an idea, get a ranked list of similar past launches from a public corpus (Y Combinator directory, Product Hunt archive, Hacker News "Show HN"), a Pydantic-validated structured comparison, a market-scope signal, and a reproducible evaluation harness benchmarking retrieval quality against a labeled public-corpus benchmark.

**Status:** Documentation-only scaffold. No build yet. Phase 1 starts after Anurag signs off on the spec and phase plan.

---

## What this is

In one line:

> Built an end-to-end production-grade startup-idea deduplication and competitor-research service: pgvector + bge-m3 retrieval, Pydantic-validated LLM structured outputs, multi-step Temporal workflows with web-search fallback, Dagster-managed corpus ingestion, Langfuse observability, and a reproducible MLflow-tracked evaluation harness (MRR / nDCG@K / calibration) over a labeled 300-idea benchmark drawn from the public YC + Product Hunt corpus.

This is the **direct, public-safe evolution of the Mercedes-Benz thesis** (LLM-based vector search, structured JSON outputs, PG vector, similarity metrics, retrieval@K). The thesis was internally scoped; this project is the same engineering pointed at a public problem, with a public corpus and a reproducible benchmark behind it.

---

## Documentation

| File | What's in it |
|---|---|
| **`SPEC.md`** | The full project spec — 12 sections, landscape table, phase plan, pitfalls, definition of done. Start here. |
| **`AGENTS.md`** | Onboarding notes for AI agents (and humans). Project structure, hard rules, "things that look like bugs but aren't." |
| **`docs/PHASE-1.md`** | Weekend 1 — FastAPI + pgvector + bge-m3 + 100-idea labeled benchmark + 4 metrics + minimal UI. Ship by Sunday night. |
| **`docs/PHASE-2.md`** | Weekend 2 — Temporal + Langfuse + MLflow + 300-idea benchmark + 3 retrieval configs. The "production-grade" weekend. |
| **`docs/PHASE-3.md`** | Weekend 3 — Dagster + calibration curve + FPR-on-novel + GitHub Actions regression + polished README + asciinema. |
| **`docs/EVAL.md`** | Deep-dive on the eval harness. The single most important part of this project. |
| **`docs/ARCHITECTURE.md`** | Why Temporal + Dagster both, the boundary between them, the Mermaid diagram for the README. |
| **`docs/LANDSCAPE.md`** | The competitive landscape — Sifft, Crunchbase, SEMrush, Gornall academic work, DeepEval, etc. The "no public tool does all of this" argument. |
| **`docs/OPERATIONS.md`** | Local dev-loop walkthrough, common failure modes, how to add a retrieval config, how to add a data source, production migration path. |

---

## The phase plan (at a glance)

| Phase | Weekend | Goal | Tier |
|---|---|---|---|
| **Phase 1** | 1 | Working idea-lookup API + UI + 100-idea labeled benchmark + 4 metrics, shipped by Sunday night. | Must-be |
| **Phase 2** | 2 | Temporal workflow + Langfuse + MLflow + 300-idea benchmark + 3 retrieval configs. The "production-grade" weekend. | Should-be |
| **Phase 3** | 3 | Dagster + calibration curve + FPR-on-novel + GitHub Actions regression + polished README + asciinema. | Can-be |

**Hard rule:** Phase 1 must be done before Phase 2 starts. Phase 2 must be done before Phase 3 starts. No backsliding. Each phase doc is the detailed task breakdown for that weekend.

---

## What success looks like

A new contributor can `git clone`, `uv sync`, `docker compose up`, drop a fresh corpus snapshot into `data/snapshots/`, run `make eval`, and reproduce the leaderboard CSV + HTML to within ±0.01 MRR. The Temporal UI shows the per-idea workflow with traces. The Langfuse UI shows the LLM calls. The MLflow UI shows the experiment sweep. The Dagster UI shows the assets + the config-change sensor firing the regression. The README explains when to pick which retrieval config. The eval harness fails the build when MRR drops below 0.7. Numbers are reproducible from the committed config.

**The CV line is the success metric.** If a change makes any of the words in that line less defensible, it's the wrong change.

---

## What's not here (intentionally)

- No `src/` — documentation-only as of now. Phase 1 will scaffold the project.
- No `docker-compose.yml` — planned in `docs/PHASE-1.md`.
- No `data/snapshots/` — committed in Phase 1.
- No `evals/` — labeled in Phase 1.
- No `results/` — generated by the eval harness in Phase 1.

The folder is intentionally bare. Tomorrow's review will decide on the build approach.

---

## Related projects in this workspace

- `../project_ideas/14_idea_competitor_dedup.md` — the source spec, from which `SPEC.md` is inlined.
- `../anuragparida.com/AGENTS.md` — the inspiration for this `AGENTS.md`'s structure (file map, "where to start" pointers, "things that look like bugs but aren't" table, hard rules).
- `../AGENTS.md` — the workspace-level rules. This project's `AGENTS.md` honors them.

---

*Last updated: 2026-06-08, documentation-only scaffold. Build starts after Anurag's review.*
